{{ pillar['headers']['salt']['file'] -}}
#
# FreeBSD ports system repository
FreeBSD-ports: {
  url: "pkg+https://pkg.woods.am/FreeBSD-ports/${ABI}",
  mirror_type: "srv",
  signature_type: "pubkey",
  pubkey: "/usr/local/etc/ssl/certs/pkg.cert",
  enabled: yes
}
